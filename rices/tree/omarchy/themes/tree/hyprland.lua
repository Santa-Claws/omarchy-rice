-- Tree theme overrides converted from the pre-Quattro Hyprland config.
local active_border_color = "rgb(6d6d6d)"

hl.config({
  general = {
    col = {
      active_border = active_border_color,
    },
  },

  group = {
    col = {
      border_active = active_border_color,
    },
  },

  decoration = {
    active_opacity = 0.85,
    inactive_opacity = 0.80,
  },
})
